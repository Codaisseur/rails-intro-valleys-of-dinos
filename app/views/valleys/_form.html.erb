<%= form_for(valley) do |f| %>
  <% if valley.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(valley.errors.count, "error") %> prohibited this valley from being saved:</h2>

      <ul>
      <% valley.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
